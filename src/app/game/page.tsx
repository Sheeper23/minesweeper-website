"use client"

import GameGrid from "@/components/GameGrid"

export default function Game() {
    return (
        <main className="w-full h-full p-8 flex">
            <GameGrid />
        </main>
    )
}